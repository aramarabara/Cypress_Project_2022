
// ----------------------------- ------ 공통 --------------------------------

// ----------------------------- 관리자페이지 접근하기 ------------------------------

export function accessAdminMenuWithId(mgt) {
    cy.get(mgt).click();
    cy.wait(500);
}